#include <string.h>
#include <stdio.h>

void left_rotate(char* x, int i)
{
  char t;
  int n, j, k;

  n = strlen(x);
}

int main(int argc, const char *argv[]) 
{
  char x[] = "abcdefgh";
  left_rotate(x, 3);
  return 0;
}
